# Overview
Build one manifest row per Markdown file, then fan out to one hydrated row per section.

## Installation
Run `uv pip install -e .` from the scaffold root before executing `uv run demo.py`.
